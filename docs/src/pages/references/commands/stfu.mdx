import { CommandArgs } from '/src/organisms/command-args';

# 治安統率機構

<CommandArgs
  versionAvailableFrom="v1.12.0"
  commandName="stfu"
  args={[
    {
      name: '個数',
      about: 'はらちょのメッセージを削除する個数',
      defaultValue: '1'
    }
  ]}
/>

第一引数で指定した個数、実行したチャンネルに存在するはらちょのメッセージを最新順から削除します。
