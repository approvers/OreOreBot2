---
title: '治安統率機構'
---

import { CommandArgs } from '@/organisms/command-args';

<CommandArgs
  availableFrom="v1.12.0"
  names={['stfu']}
  args={[
    {
      name: '個数',
      about: 'はらちょのメッセージを削除する個数',
      defaultValue: '1'
    }
  ]}
/>

第 1 引数で指定した個数、実行したチャンネルに存在するはらちょのメッセージを最新順から削除します。

:::caution
送信日時が2週間前のメッセージは Discord API の制約上削除きません。

そのようなメッセージに対して治安統率機構は機能しません。
:::
