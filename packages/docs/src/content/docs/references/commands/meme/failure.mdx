---
title: 'failure'
---

import { CommandArgs } from '@/organisms/command-args';

<CommandArgs
  availableFrom="v1.38.0"
  names={['failure', 'fail']}
  args={[
    {
      name: 'オプション',
      about: '`-k` は、失敗の部分を指定します。',
      defaultValue: '指定なし',
      availableFrom: 'v1.39.0'
    },
    {
      name: 'テキスト',
      about: 'テンプレートに挿入する1つ目のテキストです。'
    }
  ]}
/>

共同通信記者の失礼発言 MEME.

## 使用例

`!failure OreOreBotはPythonで作成しています`

```
>> 「OreOreBotはPythonで作成しています。」
「わかりました。それは一般に失敗と言います、ありがとうございます」
```

`!failure -k 終わり OreOreBotはPythonで作成しています`

```
>> 「OreOreBotはPythonで作成しています。」
「わかりました。それは一般に終わりと言います、ありがとうございます」
```

:::note
この機能は他のどのMEMEよりも使うのが難しいです。うまく使いこなせるよう練習しましょう。
:::
