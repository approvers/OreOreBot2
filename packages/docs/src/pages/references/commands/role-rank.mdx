export { Layout as default } from '../../../templates/layout';

import { CommandArgs } from '../../../organisms/command-args';

# ロール数ランキング

<CommandArgs versionAvailableFrom="v1.20.0" commandName="rolerank" />

各メンバーごとのロール所持数をランキング形式で表示します。
