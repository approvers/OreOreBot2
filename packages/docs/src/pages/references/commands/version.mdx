import { CommandArgs } from '../../../organisms/command-args';

# バージョン表示

<CommandArgs versionAvailableFrom="v1.16.0" commandName="version" />

現在のはらちょのバージョンを表示します。不具合報告の際に必要になります。

埋め込みのタイトルからはすべての OreOreBot2 のリリースページへアクセスでき、
ハイライトされたタグをクリックすると現バージョンのパッチノートにアクセスできます。
